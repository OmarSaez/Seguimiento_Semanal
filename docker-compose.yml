services:
  # Base de datos PostgreSQL
  db:
    image: postgres:16-alpine
    container_name: tracking_db
    restart: always
    environment:
      POSTGRES_DB: avance_semanal
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # Backend (Spring Boot)
  backend:
    image: omarsaez/seguimiento-backend:latest
    container_name: tracking_backend
    restart: always
    depends_on:
      - db
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/avance_semanal
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      SPRING_JPA_HIBERNATE_DDL_AUTO: update

  # Frontend (Vite + Nginx)
  frontend:
    image: omarsaez/seguimiento-frontend:latest
    container_name: tracking_frontend
    restart: always
    depends_on:
      - backend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot-etc:/etc/letsencrypt:ro
      - certbot-var:/var/lib/letsencrypt:ro
      - webroot:/var/www/certbot:ro

  # Certbot (SSL automático)
  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes:
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
      - webroot:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

volumes:
  postgres_data:
    name: tracking_db_data
  certbot-etc:
    name: tracking_certbot_etc
  certbot-var:
    name: tracking_certbot_var
  webroot:
    name: tracking_certbot_webroot
